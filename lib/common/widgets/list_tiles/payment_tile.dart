import 'package:flutter/material.dart';

class MPaymentTile extends StatelessWidget {
  const MPaymentTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const ListTile();
  }
}
